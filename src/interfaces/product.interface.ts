export interface Product {
    id: number
    title: string
    subtitle: string
    categoryID: number
    description: string
    images: string[]
    price: number
    createdAt: Date
    updatedAt: Date
}
